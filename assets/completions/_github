#!/usr/bin/env bash
# Auto-generated completion for github (from --help output)
#compdef github

local -a commands
commands=(
  'github:github subcommand'
)
_describe 'command' commands && return 0
local -a opts
opts=(
  '-b:-b option'
)
_describe 'option' opts && return 0
_files
