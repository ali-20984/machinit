#!/usr/bin/env bash
# Minimal dockutil completion — macOS dock utility common commands

#compdef dockutil

local -a cmds
cmds=(
  'add:Add an app to Dock'
  'remove:Remove an app from Dock'
  'reset:Reset Dock to defaults'
  'list:Show Dock items'
  'install:Install plist or config'
  'help:Show help'
)

_describe 'dockutil command' cmds && return 0

_files
