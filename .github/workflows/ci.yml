name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: # allow manual runs from the Actions UI
    inputs: {}

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: No-op
        run: echo "CI placeholder: lint/format/tests are run in separate workflows"
